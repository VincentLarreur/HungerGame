
// Configurations
var config = {};

// port
config.port = 3000;

// nombre de salle de jeu
config.nbRooms = 3;

// taille de la zone de jeu -> diviseur de 500 pour un bon rendu
config.taille = [50, 25, 10];

// nombre de bonbons 
config.nbBonbons = [50, 25, 5];

config.index = '/index.html'

module.exports.config = config;
